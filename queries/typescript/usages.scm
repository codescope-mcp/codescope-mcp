; Identifiers (general usage)
(identifier) @usage

; Property identifiers in member expressions
(member_expression
  property: (property_identifier) @usage)

; Type identifiers
(type_identifier) @usage

; Property identifiers in object patterns
(shorthand_property_identifier_pattern) @usage

; Property identifiers
(property_identifier) @usage
