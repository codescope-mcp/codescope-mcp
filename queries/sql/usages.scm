; SQL usages query

; General identifiers (table names, column names, function names in queries)
(identifier) @usage

; Object references (qualified names like schema.table)
(object_reference
  (identifier) @usage)
